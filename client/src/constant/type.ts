export type Student = {
    student_id?: string,
    name?: string,
    surname?: string,
    school?: string,
    year?: string,
    is_checkin?: boolean,
    is_checkout?: boolean
}

export type LoginType = {
    username?: string,
    password?: string
}